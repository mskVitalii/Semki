version: '3.9'
services:
  web:
    container_name: semki-web
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - '80:80'
    environment:
      VITE_API_URL: http://0.0.0.0:8000
    restart: unless-stopped
